# Regenerable runtime state — recreated by run_simulation.py / verify_and_tamper.py.
workspace/
# Deliberately-corrupted artifact, regenerated by verify_and_tamper.py.
packs/evidence_pack_tampered.zip
# Keep: packs/evidence_pack.zip and packs/evidence_pack_stamped.zip
# (a reader can re-run `openmiura verify` on them).
