# Work journal — 2026-07-09 00:03

## Audit + Adoptability (PyPI/GHCR + English day-2 docs)

Curro asked to audit the repo and, if healthy, do the adoptability bet
(distribution + English day-2 docs, no new runtime deps).

### Audit verdict: healthy

Multi-agent audit (4 lenses) + objective checks, all green: 10/10 CLAUDE.md
DoD, full pytest suite passing, canonical demo `success=True`, migrations 1-28
contiguous, largest production `.py` 1437 lines, README free of "openclaw"
(only internal identifiers in `docs/architecture/`, allowed), the evidence
chain complete (WHAT + WHO + WHEN). One packaging **blocker** found and fixed
first (below).

### Delivered — 6 PRs, all merged, zero new runtime deps

| PR | Commit | Content |
|---|---|---|
| [#136](https://github.com/fmarrabal/openmiura/pull/136) | `3c8d652` | **Blocker fix**: wheel shipped a broken UI — `package-data` listed only `ui/static/*` (non-recursive, so icons were already missing) while the app mounts `/ui/v2` from `openmiura/ui/v2/static` (37 files). Now `ui/static/**/*` + `ui/v2/static/**/*`; `py-modules=["app"]` KEPT (uvicorn `"app:app"` needs it — audit's drop suggestion was wrong); author email added. Proven with a clean-venv `pip install`; smoke test pins wheel contents. |
| [#137](https://github.com/fmarrabal/openmiura/pull/137) | `2a1ed61` | Dependency upper caps one major above latest (resolution unchanged); MANIFEST trims `docs/_workjournal` + `docs/_archive` from the sdist. |
| [#138](https://github.com/fmarrabal/openmiura/pull/138) | `a83639b` | `publish-pypi` job in release.yml via **PyPI Trusted Publishing (OIDC)**, gated on a stable Release + the `pypi` Environment; filtered `dist-pypi/` (wheel+sdist only). |
| [#139](https://github.com/fmarrabal/openmiura/pull/139) | `9503fc8` | **GHCR** `container.yml`: build+push on Release/dispatch, build-only on PRs touching the image. Its smoke build caught a real pre-existing bug — the Dockerfile `HEALTHCHECK` used a shell heredoc (invalid in a Dockerfile → "unknown instruction: import"); fixed to a single-line `python -c`, test guards against `<<`. |
| [#142](https://github.com/fmarrabal/openmiura/pull/142) | `fffb3e7` | New English day-2 docs: `container_image.md`, `secrets_and_keys.md`, `health_and_monitoring.md`, `upgrades.md`; linked from `docs/README.md`. Every CLI flag verified against code. |
| [#143](https://github.com/fmarrabal/openmiura/pull/143) | `ef1425b` | Translated the last 6 Spanish operator docs to English (backup_restore, production, deployment, troubleshooting, migrations, observability), preserving all code/paths/links/env-vars byte-for-byte. |

### Decisions / notes

- **Zero new runtime deps** across all six (build/twine already in the dev
  extra; `pypa/gh-action-pypi-publish`, `docker/*` actions, and the
  `python:3.11-slim` base image are CI/image concerns, not Python runtime deps).
- Publishing has **external side effects**, so PR3/PR4 add the workflows but do
  NOT publish — they are gated and fail closed until Curro completes the
  out-of-repo setup (below). I never triggered a publish.
- The `openmiura verify --tsa-anchor` / `openmiura timestamp` and RFC 3161 work
  from the prior session is orthogonal and already merged.

### Requires Curro (out-of-repo, before first publish)

1. **PyPI**: create the `openmiura` project and add a Trusted Publisher (owner
   `fmarrabal/openmiura`, workflow `release.yml`, environment `pypi`); create a
   GitHub `pypi` Environment with required reviewers. Do a TestPyPI dry run
   first. Docs: `docs/release_publication.md` §10.
2. **GHCR**: after the first image push, set the package visibility on the
   `ghcr.io/fmarrabal/openmiura` page. First run: `workflow_dispatch` with
   `tag=test`.

### Loose ends flagged (not done here)

- Pre-existing broken links in `docs/README.md` (to Phase-0-archived
  medium/roadmap files) — spawned as a separate cleanup task, out of scope.
- An untracked `tools/` dir (`tailwindcss.exe` + a css) in the working tree,
  unrelated to any of this work — kept out of every commit; gitignore or remove.

### Next

Adoptability is code-complete. Remaining is Curro's out-of-repo PyPI/GHCR setup
and the first tagged `v1.0.0` release to exercise the publish paths end to end
(TestPyPI + a `:test` GHCR tag recommended before going live).
