# Work journal — 2026-08-10 00:06

## Adversarial pass over the paper and the Medium article

Curro asked for an adversarial session over both articles, then to
regenerate them incorporating the latest pass (simulation, figures,
two-layer story, v1.1.0).

### How the review ran

A 17-agent adversarial workflow: 5 independent reviewers with distinct
lenses (facts-vs-code, overclaiming/tone, internal+cross-doc
consistency, staleness vs `main`, reader-reproducibility), each finding
then handed to a skeptic agent instructed to refute it (refute-by-default
if unconfirmable). **36 findings → 26 after dedup → top 12 verified → all
12 CONFIRMED, 0 refuted.** The remaining 14 lower-severity findings were
validated by hand; the actionable ones are fixed too.

### The two structural finds (source of truth was wrong)

Both regulatory mapping summary tables had drifted from their own rows:

- `mapping_21cfr_part11.md`: summary said Beta 7/Partial 10/Total 26;
  the table's actual rows count Beta 9/Partial 11/Experimental 2/n-a 7
  = **29 rows**. (My #158 edit updated the summary without noticing the
  base was already stale.)
- `mapping_eu_gmp_annex11.md`: summary said Beta 9/Partial 11/Total 25;
  actual rows Beta 13/Partial 14/Experimental 1/n-a 4 = **32 rows**.

Fixed both summaries to the programmatic row counts with the counting
convention stated ("one row per regulation paragraph/sub-paragraph").
The paper's §4 numbers were then restated from the reconciled tables:
Part 11 **20 of 29** rows at Beta/Partial (was "17 of 26"); Annex 11
**27 of 32** (was "19 of 25", which didn't even sum). The abstract's
"roughly half" gloss (no longer sustainable under any counting) was
replaced by "control-by-control … with an honest per-control maturity
label".

### Paper — other confirmed fixes

- **Abstract** no longer attributes the signature-grade quorum/TOTP path
  to the NMR pilot (it belongs to the release path, demonstrated in the
  executable run); recount 284 words.
- **§3.3 pack contents** now match the real ZIP (attestation/post-mortem
  exports, custody log with chain head, notarization, signed manifest) —
  the old text claimed prompt/completion pairs and a policy-YAML snapshot
  that this pack type does not embed; decision traces are noted as
  queryable in the store instead.
- **§3.3 self-verification limit** stated plainly: a fully consistent
  rewrite by an actor with unrestricted DB access passes `verify-chain`;
  the pack-attested head is what closes that for a third party.
- **§3.4 / whitepaper §3.4**: the universal negative ("there is no
  path") softened to the verified claim + open red-team audit.
- **§2**: Credal sentence neutralised (their format is not publicly
  documented → assessment not independently possible; ours is published
  so it is) — no more unverifiable "not aligned"/"marketing claim"
  assertions; "several mid-size pharma companies have stated openly"
  (uncited) → neutral conditional; "production-grade" (banned hype
  family) → "operational requirements".
- **§5**: the NMR smoke test writes a *manifest-hashed* JSON report —
  "signed" was an overclaim (its sign() records identity fields, no
  cryptography); stated explicitly and contrasted with §3.3 packs.
- **§5.1**: `--trust-anchor` semantics corrected (an unanchored verify
  of a real-key pack already reports *authoritative*; the anchor binds
  the signer; a foreign anchor demotes to non-authoritative, exit 2) and
  the Fig. 4 caption no longer claims four *distinct* exit codes.
- **§6.4**: empirical base updated to the two artefacts (smoke test +
  executable run), with the faithfulness-vs-efficacy line made explicit.
- 15 → **16** admin sub-routers; `cryptography` + `asn1crypto` named for
  offline verification.

### Medium — regenerated

- The reader can now actually verify something: the article points at
  the **committed pack** (`docs/academic/simulation/packs/`) — the old
  draft told readers to verify a pack they had no way to obtain.
- Honest two-anchor story for `verify-chain` (consistent-rewrite caveat
  + pack-attested head), same as the paper.
- Concrete trust anchor: the CLI accepts a 64-hex fingerprint; the
  committed pack's fingerprint is cited from `verify_summary.json`
  (labelled as the demo operator key). `operator.pem`/`tsa.pem`
  hypotheticals removed; the TSA cert is the downloadable FreeTSA cert.
- "Try to break it" now uses steps that work as written (the simulation
  leaves `workspace/audit.db` + config on disk; `verify-chain --config …`)
  and distinguishes the trigger-refused `events` edit from the
  chain-caught `release_approvals` edit.
- Exit-code note completed (0/1/3); every command evidenced by captured
  runs.

### New evidence captured

`verify_and_tamper.py` now fetches the TSA's public certificate and runs
`openmiura verify --tsa-anchor` for real: captured result
`reported_trusted: true, exit 0` (freetsa.org cert). Driver re-run
refreshed the stamped pack (new genTime 2026-08-09T22:03:56Z) and
figures. The raw result JSONs keep the deprecated internal adapter
identifier verbatim (sanitising captured evidence would defeat it);
noted in the simulation README, prose cleaned.

### Verification

Full `pytest -q` suite green before push (see PR CI). DoD word counts
hold: paper 4,374; whitepaper 4,075.

### Next step

PR with this journal; merge on green. The regenerated Medium draft is
ready for Curro's edit/publish; the paper for his arXiv formatting pass.
