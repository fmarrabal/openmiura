# 2026-05-06 22:59 — Fase 0 (Limpieza y verdad) cerrada

## Qué se hizo

Rama: `phase/0-cleanup-and-truth` (creada desde `main @ c0d8079`).

Commits en orden:

1. `4495f04 chore(repo): commit master prompt and ignore local-only artifacts` — añade `CLAUDE.md` al repo y `.gitignore` para `.claude/` y `demo_artifacts/`.
2. `ca8ce5e docs(agents): add AGENTS.md disclosing LLM-assisted development` — disclosure explícito de desarrollo asistido por LLM.
3. `062d81e docs(readme): rewrite README from scratch, hype-free` — README desde cero, status `experimental`, sin marketing-speak, sin OpenClaw, con quick start, arquitectura ASCII y enlaces a `AGENTS.md` y `LICENSE`.
4. `beab7d1 refactor(adapters): rename openclaw module to runtime_adapters/external` — rename de `openmiura/application/openclaw/` a `openmiura/application/runtime_adapters/external/`. 22 imports actualizados (3 application + 3 scripts + 16 tests). Identificadores internos `OpenClawAdapterService` / `OpenClawRecoverySchedulerService` documentados como legacy en el `__init__.py`. API HTTP `/admin/openclaw/*` intacta.
5. `494856f docs(public): remove OpenClaw narrative from live public docs` — limpia OpenClaw en 10 docs vivos: `public_narrative.md`, `walkthroughs/canonical_runtime_governance_walkthrough.md`, `demos/canonical_demo.md`, `demos/payloads/runtime_create.json`, `enterprise_alpha.md`, `alpha_release_checklist.md`, `github_pr_merge_publish_checklist.md`, `media/screenshot_plan.md`, `quickstarts/developer.md`, `sprint5_refactor_notes.md`.
6. `e4fcb7b docs(archive): move legacy marketing and refactor docs under docs/_archive/` — mueve 13 docs históricos (8 marketing-speak + 5 refactor docs) a `docs/_archive/`. README del archivo añadido explicando propósito.
7. `74bf188 chore(config): neutralize default system prompt and gitignore personal persona` — `configs/openmiura.yaml` con system prompt neutro/profesional. Persona Curro/MiuraBot preservada como copia en `configs/personal/curro.yaml`. `configs/personal/` en `.gitignore`.

## Estado de la DoD de Fase 0

- [x] `AGENTS.md` existe (commit 1) y está enlazado desde `README.md` (commit 2).
- [x] El nuevo `README.md` no contiene "OpenClaw", "enterprise-grade", "production-ready", "world-class", "MiuraBot".
- [x] `git grep -i 'openclaw' README.md` = 0; en `docs/` = 0 en docs vivos (el `_archive/` retiene las referencias como historia, decisión consensuada).
- [x] `pytest -q --tb=no -p no:cacheprovider tests/unit` → 27/27 pasan tras cada commit que toca código.
- [x] `python scripts/run_canonical_demo.py` → `success=True` tras cada commit que toca código.

DoD adicional auto-impuesta:

- [x] `configs/openmiura.yaml` público no menciona "Curro", "Miura", "MiuraBot".

## Decisiones tomadas y por qué

- **Rama desde `main`, no del worktree.** La rama `phase/0-cleanup-and-truth` no existía cuando arrancamos. Se creó desde `main @ c0d8079` para tener una línea base limpia. El worktree `claude/sharp-hodgkin-7e7fef` quedó abandonado.
- **`CLAUDE.md` commiteado en la rama Fase 0**, no directamente en `main`. Mantiene la rama autocontenida y la introducción del documento maestro queda registrada en el mismo cambio que dispara la limpieza.
- **API HTTP `/admin/openclaw/*` no renombrada.** Hay 14+ rutas `@router.get/post("/admin/openclaw/...")` en `openmiura/interfaces/http/routes/admin.py`. Renombrar rompería el contrato público. El CLAUDE.md ya prevé que los identificadores internos persistan como deprecated y se renombren en una migración futura controlada.
- **Identificadores internos `OpenClawAdapterService`, `OpenClawRecoverySchedulerService` no renombrados.** Mismo motivo. Documentados como legacy en el docstring del `__init__.py` del nuevo paquete `runtime_adapters/external/`.
- **Marketing-speak archivado intacto** (commit 5) en lugar de limpiarlo antes. El `ROADMAP_24M_PRODUCT_STRATEGY.md` tenía 40+ menciones estructurales de OpenClaw (secciones enteras tipo "OpenClaw Compatibility Adapter v1"); limpiarlo lo dejaba mutilado para un documento que va al cementerio. Curro confirmó la interpretación de que el `_archive/` es histórico y exento del DoD literal.
- **Docs `docs/refactor/openclaw_*` archivados completos** en `docs/_archive/refactor/` aunque el CLAUDE.md no los listaba explícitamente. Eran documentación del módulo `openclaw` con su path antiguo; tras el rename ya no representan estado vivo y mantenerlos confunde más que aclara.
- **`configs/personal/curro.yaml` creado como copia completa** del `openmiura.yaml` original con persona Curro/MiuraBot. Gitignored. Curro lo aplica con `--config configs/personal/curro.yaml` cuando arranca su instalación personal.
- **Python interpreter** registrado como nota de memoria: el `python` por defecto del `PATH` es 3.9.13 (Microsoft Store) y revienta con el código por la sintaxis PEP 604 (`str | None`). El intérprete operativo en esta máquina es el conda env `openmiura-clean` (Python 3.11.15, pytest 9.0.2). Memoria persistente registrada.

## Riesgos detectados que Curro debe revisar

1. **Endpoints HTTP `/admin/openclaw/*` siguen como API pública.** La documentación ya no los menciona pero los endpoints existen. Cualquier consumidor externo aún ve esos paths. *Acción sugerida:* planificar la migración futura controlada (mencionada en CLAUDE.md §4 Fase 0 punto 3) como rama dedicada o sub-tarea de Fase 1, con redirect + deprecation header.
2. **Identifiers `OpenClawAdapterService` / `OpenClawRecoverySchedulerService` y la columna/clave correspondientes en BD.** Cuando se renombren, hay 22 sitios de import (los mismos de commit 3) + tablas de BD a tocar. Migración no trivial.
3. **Docs en `_archive/` no editados** contienen narrativa OpenClaw obsoleta. El `README.md` del archivo lo aclara, pero un lector externo que llegue al `_archive/` por GitHub puede confundirse antes de leer ese README. *Acción sugerida:* añadir un banner "ARCHIVED — see top-level README" como primera línea de cada archivo en `_archive/` si te preocupa. (No lo hago sin pedirlo).
4. **Tests con nombre `test_openclaw_*.py`** (16 archivos). No tocados; mantienen nombre por estabilidad. Si en el futuro quieres alinear el naming, es una tarea cosmética separada.
5. **`configs/personal/curro.yaml` no commiteado.** Existe local pero no en remoto. Si cambias de máquina y olvidas copiarlo, pierdes la persona personal. *Acción sugerida:* backup fuera del repo (gestor de contraseñas, vault, etc.).
6. **No hay test automatizado** que verifique que el system prompt público no contiene "Curro", "MiuraBot" o similares. Una regresión futura podría reintroducirlos sin que CI proteste. *Acción sugerida ligera:* test en `tests/unit/test_config_public_prompt.py` que cargue `configs/openmiura.yaml` y haga `assert "Curro" not in default_system_prompt` etc. Puedo añadirlo en una tarea separada si quieres.

## Próximo paso recomendado

1. **Push de `phase/0-cleanup-and-truth`** y abrir PR contra `main` para tu revisión. Mensaje de PR: enlazar a este workjournal.
2. Cuando merguees Fase 0, arrancar **Fase 1** según §4 del CLAUDE.md desde `phase/1-refactor-audit-store` (rama nueva desde `main` post-merge):
   - Sprint 1: extracción de `voice_repo` (27 métodos, dominio aislado, menos riesgo) y `evaluations_repo` (7 métodos).
   - Patrón: extracción incremental con fachada. `AuditStore` queda como API pública delegando en repos especializados.
   - Verificación tras cada extracción: `pytest tests/unit tests/test_phase{1,2,3,4,5}*.py --tb=short` + canonical demo. Si algo rompe, rollback inmediato.

## Notas sobre la sesión

- Tiempo total invertido en Fase 0: una sesión larga (no medido con precisión).
- 7 commits, 0 retrocesos.
- 4 puntos de pausa-y-pregunta a Curro para decisiones no resueltas por el documento maestro: rama / CLAUDE.md / `.gitignore` / cantidad de commits; alcance del archivado vs limpieza inline; `_archive/` exento del DoD; sí/no a re-redactar el alcance de commit 4. Todos resueltos sin reabrir el plan.
