# Work journal — 2026-07-13 00:30

## v1.1.0 shipped — first release through the full publish pipeline

Curro asked for the publication setup + first release end-to-end ("hazlo tú").
Split of duties, honestly held: Curro registered the PyPI Trusted (pending)
Publisher on pypi.org and approved the `pypi` deployment; everything else was
executed from the session.

### What happened, in order

1. **GitHub Environment `pypi`** created via API with Curro as required
   reviewer — the manual approval gate for PyPI publishes.
2. **GHCR `:test` image** pushed via `container.yml` workflow_dispatch —
   container path proven end-to-end before the real release.
3. **release.yml dry run** (`release_tag=v1.0.0`, `publish_to_release=false`):
   quality gate + reproducible artifacts green, `publish-pypi` correctly
   skipped — the gating works.
4. **Key finding**: tag+Release `v1.0.0` already existed (2026-04-24,
   pre-dating the publish workflows) and `main` was ~45 PRs ahead with a stale
   `1.0.0` version. Shipping today's main as "1.0.0" again would be dishonest →
   **v1.1.0** (minor: public API kept back-compatible throughout).
5. **Version bump** ([#149](https://github.com/fmarrabal/openmiura/pull/149),
   `ca70f33`): pyproject + `__init__` + the installation-smoke pins +
   `docs/installation.md`. Domain-data "1.0.0" fixtures untouched.
6. **Tag + [Release v1.1.0](https://github.com/fmarrabal/openmiura/releases/tag/v1.1.0)**
   with honest, no-hype notes. Both workflows fired:
   - `container.yml` → GHCR image `1.1.0` / `1.1` / `latest` / `sha` ✅
   - `release.yml` → quality gate + reproducible wheel/sdist/bundle uploaded to
     the Release ✅; `publish-pypi` waited on the Environment gate, Curro
     approved → **published to PyPI via OIDC** (no token), converting the
     pending publisher and claiming the `openmiura` name ✅
7. **Adoption proof**: clean venv → `pip install openmiura==1.1.0` from real
   PyPI → CLI runs, `__version__ == 1.1.0`, all 37 `ui/v2` static files land in
   site-packages (the #136 wheel-content fix, verified in production).

### README

Quick start now leads with `pip install openmiura` + a minimal config that was
**tested verbatim** against the PyPI-installed package (bare `openmiura doctor`
without a config fails by design — the README shows the config-file flow), plus
the container one-liner, with the dev/demo path kept below.

### Remaining (optional, Curro)

- GHCR package visibility (private by default; UI-only flip).
- Future releases repeat this pipeline automatically: bump version → tag →
  Release → approve the `pypi` deployment.
